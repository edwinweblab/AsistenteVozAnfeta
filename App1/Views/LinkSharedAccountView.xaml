<Page
    x:Class="Anfeta.UI.Views.LinkSharedAccountView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Login Shared (Anfeta)"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>

        <TextBox Grid.Row="1"
                 Header="Usuario"
                 Text="{Binding User, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,12"/>

        <PasswordBox x:Name="PassBox"
                     Grid.Row="2"
                     Header="Contraseña"
                     PasswordChanged="PassBox_PasswordChanged"
                     Margin="0,0,0,12"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
            <Button Content="Iniciar sesión"
                    Command="{Binding LoginSharedCommand}"/>
            <Button Content="Cerrar sesión"
                    Command="{Binding LogoutSharedCommand}"/>
        </StackPanel>

        <TextBlock Grid.Row="4"
                   Text="{Binding StatusMessage}"
                   Margin="0,16,0,0"
                   TextWrapping="Wrap"/>
    </Grid>
</Page>
